from tkinter import *

def process():
    pass
def change_img():
    pass
    #path = inputBox.get()
    #img = PhotoImage(file = path)
    #imageLabel.configure(image = img)
    #imageLabel.image = img

if __name__ == "__main__":

"""
    win = Tk()
    win.title("Raspberry Pi UI")
    win.geometry('200x100+200+200')

    def clickMe():
        label.configure(text=str.get())
        action.configure(text=str.get())


    label = Label(text='InputText : ')
    label.grid(column=0, row=0)
    str = StringVar()

    textbox = Entry(win, width=20, textvariable=str)
    textbox.grid(column=0, row=1)
    action = Button(win, text="Change Text", command=clickMe)
    action.grid(column=0, row=2)
    win.mainloop()
"""
"""
    window = Tk()

    #indow.config(width = 680, height = 480, bg = 'gray')
    window.geometry("680x480+0+0")
    window.resizable(0,0)

    photo = PhotoImage(file = '우주소녀.gif')
    imageLabel = Label(window, image = photo)
    imageLabel.pack()

    inputBox = Entry(window)
    inputBox.pack()

    button = Button(window, text = "클릭", command = change_img)
    button.pack()

    window.mainloop()
"""

